import { Typography } from "antd";

const Organizations = () => {
  return (
    <div>
      <Typography
        style={{
          color: "#fff",
        }}
      >
        Just a list of all organizations created
      </Typography>
    </div>
  );
};

export default Organizations;
